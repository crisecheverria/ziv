//! The status line (mode indicator, filename, dirty marker, cursor position) and
//! the command/message line below it (echoes ":" input, pending command, search).
pub const StatusLine = struct {
    // TODO Phase 7: render(editor, *Screen, row) into the bottom rows.
};
