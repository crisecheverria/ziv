//! ":" command-line commands (`:w`, `:q`, `:wq`, `:e {path}`, `:42` goto line)
//! and "/" search. Parses the command-line buffer and dispatches.
pub const Ex = struct {
    // TODO Phase 5: parse + execute ex commands; "/" search with n / N.
};
